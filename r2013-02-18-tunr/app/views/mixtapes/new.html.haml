.row
  .five.columns
    = render(:partial => '/shared/errors', :locals => {:object => @mixtape})
    = form_for @mixtape do |f|
      = f.label :name
      = f.text_field :name, :autofocus => true
      = f.submit nil, :class => 'success button radius'
  .seven.columns